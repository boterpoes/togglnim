togglnim
========

Togglnim is a wrapper around the Toggl REST API written in Nim.

At the moment this project isn't in a usable state, as only a couple of API
endpoints have been implemented yet, and none of them deal with time entries.
